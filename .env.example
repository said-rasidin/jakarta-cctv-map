# Opt-in browser-only object detection experiment
NEXT_PUBLIC_ENABLE_CCTV_AI=false

# Server-side CARTO basemap key: https://carto.com/basemaps/apikey/
CARTO_BASEMAP_API_KEY=
# Legacy alias supported if it contains a basemap key (not an account token):
# CARTO_API_ACCESS_TOKEN=
